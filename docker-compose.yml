services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ${PWD:-.}:/workspace
      # Persist .lake cache across runs
      - lake-cache:/workspace/.lake
    working_dir: /workspace

volumes:
  lake-cache:
